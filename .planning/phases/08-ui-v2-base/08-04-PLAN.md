---
phase: 08-ui-v2-base
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03
files_modified: []
autonomous: false
requirements:
  - UIS-01
  - UIS-02
  - UIS-03
  - UIS-04
  - UIX-07

must_haves:
  truths:
    - "Plugin window opens at 592×600px (confirmed visually wider than v1 320×480)"
    - "Audit-1 empty state shows headline, subtitle, and red 'Audit & Inject' CTA"
    - "Audit-2 results state shows status banner, metric cards, accordion categories, and 'Re-audit & Inject' CTA"
    - "Config tab shows 'MCP Server Setup', 5 numbered steps, code block, tools section, and 'Export variables in JSON' CTA"
    - "Font is monospace throughout all screens"
    - "User confirms 'approved' at the checkpoint"
  artifacts:
    - path: "packages/plugin/dist/ui.html"
      provides: "Bundled plugin UI (vite-plugin-singlefile output)"
    - path: "packages/plugin/dist/code.js"
      provides: "Sandbox entry point"
  key_links:
    - from: "packages/plugin/manifest.json"
      to: "packages/plugin/dist/code.js"
      via: "main field in manifest"
      pattern: "dist/code.js"
    - from: "packages/plugin/manifest.json"
      to: "packages/plugin/dist/ui.html"
      via: "ui field in manifest"
      pattern: "dist/ui.html"
---

<objective>
Human verification that all three Figma screens render correctly in the plugin.

Purpose: Automated tests verify state logic and TypeScript correctness. Only a human can confirm pixel-faithful rendering against the Figma frames. This checkpoint is the Phase 8 acceptance gate.

Output: User confirms visual accuracy or reports issues for Plan 05 gap closure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ui-v2-base/08-CONTEXT.md
@.planning/phases/06-discovery-architecture-decisions/UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the plugin for Figma</name>
  <files>packages/plugin/dist/ui.html, packages/plugin/dist/code.js</files>
  <action>
    Build the plugin so the user can load it in Figma Desktop.

    Run from the repo root:
    ```
    cd packages/plugin && npm run build
    ```

    If build fails, diagnose and fix TypeScript or bundler errors before proceeding. Do not pass this task until the build succeeds.

    After a successful build, report:
    - Build output directory (typically `packages/plugin/dist/`)
    - That `dist/ui.html` exists (vite-plugin-singlefile bundles everything into one HTML file)
    - That `dist/code.js` exists (sandbox entry point)
  </action>
  <verify>
    <automated>cd D:/programacion/proyectos/ai-ready-ds-auditor/packages/plugin && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Build exits with code 0; dist/ui.html and dist/code.js exist</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual accuracy verification in Figma</name>
  <action>Pause for human to load the built plugin in Figma Desktop and verify all three screens match the Figma design frames.</action>
  <verify>
    <automated>MISSING — visual accuracy requires human verification in Figma</automated>
  </verify>
  <done>User types "approved" confirming all three screens render correctly</done>
  <what-built>
    Three plugin screens rebuilt to match Figma frames:
    - Audit-1 (empty state): welcome headline, subtitle, 'Audit &amp; Inject' CTA
    - Audit-2 (results state): status banner, MetricCards, summary sentence, Accordion categories, 'Re-audit &amp; Inject' CTA
    - Config-1 (configuration): 'MCP Server Setup' title, 5 numbered steps, code block, tools section, 'Export variables in JSON' CTA
    Plugin window is now 592×600px (was 320×480)
  </what-built>
  <how-to-verify>
    1. Open Figma Desktop
    2. Go to Plugins → Development → Import plugin from manifest
    3. Select `packages/plugin/dist/manifest.json` (or `packages/plugin/manifest.json` if manifest points to dist/)
    4. Open any Figma file that has a Design System
    5. Run the plugin

    **Screen 1 — Audit tab (empty state):**
    - Plugin window should be noticeably wider than before (592px not 320px)
    - Headline: "Welcome to AI-Ready DS Auditor!" visible and centered
    - Subtitle: "Press the button below to get started" below the headline
    - Red "Audit &amp; Inject" button at the bottom of the empty area
    - Tab bar at top shows "Audit" (active) and "Config" tabs

    **Screen 2 — After running audit:**
    - Click "Audit &amp; Inject"
    - Wait for audit to complete
    - Results should show: status banner (green "AI Context injected - Ready for IDE"), two metric cards (Variables + Components), summary sentence, accordion categories (Color, Typography, Spacing, etc.)
    - "Re-audit &amp; Inject" button at bottom

    **Screen 3 — Config tab:**
    - Click the "Config" tab
    - "MCP Server Setup" heading visible
    - 5 numbered steps visible
    - JSON code block visible with correct MCP configuration
    - "Available MCP tools" section visible
    - "Export variables in JSON" button at bottom (disabled if no audit yet)

    **Visual language check:**
    - Font is monospace throughout (not Inter or system sans-serif)
    - Colors match Figma: red button (#F55442), dark green success banner (#2B3C35)
    - Generous whitespace — not cramped
    - Accordions open by default in results view
  </how-to-verify>
  <resume-signal>
    Type "approved" if all three screens look correct.
    Or describe the specific visual issues found (e.g., "accordion overflows horizontally", "wrong font", "button color incorrect") so they can be fixed.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 8 is complete when:
1. Build succeeds
2. User types "approved" at the checkpoint
</verification>

<success_criteria>
- Plugin opens at 592×600px
- Audit-1 empty state matches Figma Audit-1 frame: headline + subtitle + red CTA
- Audit-2 results state matches Figma Audit-2 frame: status banner + metric cards + accordions + CTA
- Config-1 matches Figma Config-1 frame: MCP steps + code block + tools + CTA
- Visual language is consistent: monospace font, red brand color, generous whitespace, tabs on top, CTA at bottom
- User confirms "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-v2-base/08-04-SUMMARY.md`
</output>
