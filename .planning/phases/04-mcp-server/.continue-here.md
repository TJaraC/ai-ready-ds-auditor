---
phase: 04-mcp-server
task: text-style-wiring
status: in_progress
last_updated: 2026-03-04T21:00:00.000Z
---

<current_state>
extractTextStyleTokens() committed in tokens.ts but NOT yet wired in index.ts.
index.ts still only calls extractVariableTokens(). Need to import and call
extractTextStyleTokens(textStyles) and merge results with variable tokens.
</current_state>

<completed_work>
- 04-01, 04-02, 04-03: all complete and verified working ✓
- fix: pluginData nested by plugin ID — store.ts now reads [PLUGIN_ID] correctly ✓
- fix: getVariableCollectionByIdAsync for dynamic-page ✓
- fix: void variables → extractVariableTokens() ✓
- feat: extractTextStyleTokens() added to tokens.ts ✓ (committed cc4c3d0)
- Claude Desktop MCP config: token=figd_lkOcLefD7kQvCEQ9UC1VxeEqiH7QfRsxusLw5tyH, fileKey=96Pfz0V9pxJ0CYOLWY5rWT
- get_design_tokens VERIFIED WORKING in Claude Desktop ✓
</completed_work>

<remaining_work>
1. Wire extractTextStyleTokens in index.ts:
   - Import: import { extractVariableTokens, extractTextStyleTokens } from './tokens';
   - textStyles already loaded in PASS 0: const [paintStyles, textStyles, effectStyles, variables]
   - Add after extractVariableTokens: const textStyleTokens = extractTextStyleTokens(textStyles);
   - Merge: assembleReport(allIssues, allComponents, [...tokens, ...textStyleTokens], ...)
2. npm run build -w packages/plugin
3. User re-runs plugin in Figma → Inject / Update
4. Verify typography tokens appear in get_design_tokens
5. Write final 04-03 note / update STATE.md for Phase 5
</remaining_work>

<next_action>
Edit index.ts: add extractTextStyleTokens to import, call it with textStyles (already loaded),
merge into tokens array before assembleReport. Then build.
</next_action>
